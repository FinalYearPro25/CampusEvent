import React from 'react'
import Layout from '../components/Layout';
import AttendingContent from '../components/AttendingContent';

export default function MyEvents() {
    return (
        <Layout>
            <AttendingContent/>
        </Layout>
      );
}
